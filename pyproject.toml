[project]
name = "py"
version = "0.1.0"
description = "python的学习工程"
requires-python = ">=3.14"

dependencies = [
    "requests>=2.26.0",
    "flask>=3.1.3",
    "ruff>=0.15.15",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]

